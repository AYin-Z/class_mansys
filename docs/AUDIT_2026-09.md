# 全系统业务逻辑 / 前端逻辑审计报告（2026-09-11）

审计方式：7 个并行只读审计切片（请假 / 班费 / 内容模块 / 横切安全 / 前端基础 / 前端页面 / 数据层与脚本）
+ 我自己复核横切层与 Agent/MCP/渠道。全程只读，结论均带 `文件:行号` 证据；关键条目已在生产/测试库实测复现。

## 0. 总体结论

系统**不是一堆零散 bug，而是 5 个系统性根因**外溢的结果（下面每个根因都能对上多条缺陷）：

| # | 根因 | 典型后果 |
|---|---|---|
| R1 | 早期「role>=1 就是管理员」的粗粒度判断，没跟上后来的权限矩阵/作用域体系 | 干部可改他人邮箱→验证码接管超管；跨区队审批、改删他人内容 |
| R2 | 「能看」与「能写」的作用域用同一个函数（`canAccessClassRecord` 是中队平行查看语义），写在读基本都做了、写大量遗漏 | 任一区队干部可改删他区队通知/公告/作业/班费/相册/积分 |
| R3 | 审计中间件把请求体全量入库，读取口又对全体干部开放 | 心理咨询、匿名建议被去匿名化（线上已有实证） |
| R4 | 「人数」口径三分裂：`COUNT(*) FROM users`（215） vs `member_type='student'`（205） | 大额报销投票门槛多 7 票且永久卡死、班费应收虚增 |
| R5 | 写库路径缺事务/状态机/幂等键，失败静默 | 先写后鉴权、重复提交、驳回后幽灵待办、半恢复备份 |

审计期间已修复并上线（见文末「已修复」），其余按 P0→P1 分批处理建议见 §4。

## 1. P0（必须优先，含已修）

| 状态 | 缺陷 | 证据 | 影响 |
|---|---|---|---|
| ✅已修 | 用户资料接口越权 → 账号接管链 | `routes/users.js`(旧版) `authorizeAdmin`=`role>=1`；`models/User.js:78` 允许改 email/phone；`routes/auth.js:45` 邮箱验证码登录 | 已实测：区队长改超管邮箱→`email-code-login`→拿到 role=8 令牌 |
| ✅已修 | 助手写操作可绕过二次确认（模块工具） | `services/agent/toolCatalog.js` 模块工具无 `write` 标记；`agentService.js:90,220` 只判 `tool.write` | 模型改用 `POST /api/points` 形式的模块工具即直接写库 |
| ✅已修 | 审计日志泄露匿名内容 | `middleware/operationLog.js:70-75` 存 body；`routes/admin.js:22`+`AdminController.js:169-183` 仅 `VIEW_ROSTER`(角色1-9) 并返回 detail | 线上 `operation_logs.id=1107` 含心理正文；建议正文/验证码同样入库 |
| ⏳待修 | `/uploads` 生产实际无鉴权 | `.env:28 UPLOAD_AUTH_MODE=compat` + `middleware/uploadAuth.js:14-20` 直接放行 | 127 份请假证明、凭证等拿到 URL 即可匿名读；且 `media.ts:12` 把 24h JWT 拼进 URL 并落日志 |
| ⏳待修 | 班费写/审批面几乎无作用域 | `FeeController.js:70-97,175-234`；`models/ExpenseApproval.js:156-170`（SQL 无 class_id） | 任意学员可给别区队批次缴款（金额客户端可控，可为负）；任一区队长可审批/驳回他区队报销；列表泄露他区队金额 |
| ⏳待修 | 大额报销永久卡死 | `models/ExpenseApproval.js:4-11` 阈值=`COUNT(*) FROM users`×2/3；`routes/fee.js:26-30` 投票锁在 `APPROVE_FEE_USE`(1-9)，学员无入口 | 215 人→144 票，单区队不可能达到；>500 元报销无法通过 |
| ⏳待修 | 通知完成名单跨区队泄露 | `models/Notice.js:144-146` 无 class 过滤 | 任一区队长看到全中队 205 人姓名+学号，完成率分母全错 |
| ⏳待修 | 积分先写库后鉴权 | `controllers/PointsController.js:44-56`（INSERT 在 403 之前） | 越权加分真实生效、可无限刷；线上已有脏数据 id=132/133/134（-100/+1000/-10000，用户25 被刷成 -9050） |
| ⏳待修 | 班费公示不分区队 | `models/FeePublication.js:7-25` 两段 SQL 无 class_id | 本区学员看到全校收支与他区 50 条支出明细 |
| ⏳待修 | 审批结果恒报成功 | `FeeController.js:194,212` `success:true` 硬编码 | 并发/重复点击时界面说「已审批通过」，实际未落库 |
| ⏳待修 | seed.js 半清空且无回滚 | `seed.js:35-52` 清理清单缺 5 张 FK 表 + `catch(e){}` 吞错 + 无事务 | 删完一半业务表后中断（历史上本系统已发生过生产库被误清空） |

## 2. P1（按主题归并）

**权限与作用域**
- 角色写进 24h JWT 且中间件不查库（`services/authService.js:90-94`、`middleware/auth.js:12-18`、`shared/scope.js:20`）：被降级/移出的用户在令牌过期前保留旧权限。
- 写路径缺记录级作用域：作业批改（`HomeworkController.js:80-89`）、抽奖开奖（`LotteryController.js:69-87`）、擂台判定（`ChallengeController.js:93-181`）、相册审核（`AlbumController.js:161-197`）。
- 心理申请可见范围过宽（`routes/psychological.js:11,13`，`HANDLE_PSYCHOLOGICAL` 默认角色 1-9）。
- `/api/auth/find-by-student` 可枚举全校手机号/邮箱（`AuthController.js:288-298`，`_publicUser` 含联系方式）+ 登录报错差异化。
- 注册可自选区队（`AuthController.js:200-233`），注册即可读该区队通知/相册/公示。

**业务正确性**
- 作业截止后可提交且覆盖已批改分数/评语（`models/Homework.js:49-70`）。
- 抽奖可重复开奖/对已关闭活动开奖；擂台无唯一键、judge 不校验状态（可刷战绩、乱换擂主）。
- 三个角标计数无区队过滤（`models/Notice.js:68-77,126-132`、`models/Homework.js:100-111`）：红点数恒大于列表条数。
- 「逾期未销假」恒为 0：`models/Leave.js:126-131` 的 autoCancel 谓词与 `CompanyController.js:61-63` 的统计谓词完全相同。
- 「今天」用 UTC：`CompanyController.js:21,147` `toISOString().slice(0,10)` → 北京时间 00:00-08:00 查的是昨天（早操时段必现）。（本轮已修）
- 余额字符串拼接：`models/Fee.js:71-75` DECIMAL SUM 为字符串，`income + collected` 变成拼接（线上暂无「收入」行故为潜伏，`totalExpense` 已返回字符串）。
- 收缴口径混乱：`FeeCollection.js:7,17,30` 用全库人数；免缴写 `paid_at` 被计入已缴；重复缴纳覆盖金额而非累加；`collected_amount`（3685.80）与 0 条明细账实不符。
- 报销 `type` 无枚举校验（`FeeController.js:116`），批准了却不进任何口径。
- 驳回后残留 `status=0` 节点 → 幽灵待办（`ExpenseApproval.js:173-181` + `getPendingApprovals` 不过滤 `e.status`）。
- 已上线库大额审批链缺 step 2（`seed.js:157-159`）：680/2500 元报销永久卡死。

**数据与运维**
- 跨表写入无事务：`castVote`（票已投状态未落）、`createExpense`（INSERT→createChain→stampClassId 三次写库，失败留半成品且 `class_id IS NULL` → 全局可见）。
- `restore-db.js` 无库名白名单、无事务、时区敏感（TZ=UTC 机器恢复会让 TIMESTAMP 偏移 8 小时）——比测试夹具的护栏还弱。
- `setup-test-db.js` 可 DROP 任意库且迁移清单硬编码缺 015/016。
- 迁移语义非幂等：重跑 016 复活被撤销的权限；重跑 010 把有意为全局（class_id NULL）的内容重新打上区队标记。
- 备份不含视图/例程，恢复只覆盖 dump 内的表；保留策略硬编码库名。
- `user_stats` 视图 LEFT JOIN 交叉膨胀（实测 total_points=100，真值 50）。
- 缺失索引：`users.member_type`、`leaves(status,is_cancelled,end_time)`、`expenses(status/created_at)`、`fee_collection_records(paid_at)`、`notices(class_id,created_at)`。
- 缺失唯一约束：`homework_submissions(homework_id,user_id)`、`challenge_applications(challenge_id,user_id)`、`leaves` 同人同时段、`fee_collections` 同区同批次、`companies(name)`。
- `database_init.sql` 不可重复执行且与迁移脱节（缺 015/016/012/013 的表列）。
- `import-roster.js` 新建中队路径必失败（`companies.id` 为 VARCHAR 无自增）且会把所有区队 company_id 改写。

**前端**
- 请求层错误提示是死代码（`utils/request.ts` 调 `window.uni?.showToast`，全仓未定义）+ 33 处空捕获 → 失败静默。（本轮已修）
- 401 只清 token/profile，不清权限快照 → 换用户后沿用上一用户入口。（本轮已修）
- 超管后台 3 个导出按钮必然 401（`window.open` 不带 Authorization）。（本轮已修，改带鉴权 blob 下载）
- 路由守卫只看 `requiresAdmin`，权限矩阵在 UI 不生效；`fee/index.vue:13` 硬编码角色。
- `/pages/fee/approvals` 漏 `requiresAdmin`；`uploadFile` 无 401/进度；fetch 无超时。
- `types/roles.ts` 权限镜像与后端默认矩阵漂移且缺 10 个键。
- `utils/ui.ts` 用 rpx，浏览器全部丢弃（toast/确认弹窗排版塌）。
- agent 页流式失败留空气泡；SpeechRecognition 不 stop。

## 3. 已核对正常（可直接采信）

- SQL 注入面：全仓动态 SQL 片段只剩常量/白名单表列，用户输入全部 `?` 占位——无可注入点。
- XSS：4 处 `v-html` 全部经 `sanitizeHtml`/`renderMarkdown` 白名单，未发现 bypass。
- MCP/令牌链路：sha256 存储、可吊销、写操作两阶段、`agent_actions` 有 TTL 与归属校验，无 IDOR。
- 错误契约与 5xx 不泄堆栈；CORS 白名单、helmet、trust proxy、限流顺序正常。
- 请假/班费/通知等**只读**接口作用域基本齐全（与 E2E 断言一致）。
- 权限矩阵以 DB 为准并 30s 兜底刷新；防锁死保护有效。
- 迁移 005-016 的加列加索引均幂等（信息模式前置判断）；`migrate.js` 的 sha256 + dry-run 实现正确。

## 4. 建议修复批次

- **批次 1（P0 收口，建议立即）**：班费模块全套（作用域+金额服务端取值+审批链事务+公示按区队+阈值口径）、通知完成名单与角标口径、积分先鉴权、统一「在编人数」helper。
- **批次 2（隐私与数据安全）**：`/uploads` 切 strict + 短时效签名票据 + 日志 redact `req.url`；`find-by-student` 收敛；注册审核。
- **批次 3（一致性）**：补唯一键与索引、`user_stats` 视图修正、`database_init.sql` 与迁移对齐、seed/restore/setup 三个脚本加护栏与事务、迁移语义幂等。
- **批次 4（前端体验与权限一致性）**：路由 meta 接权限快照、镜像以服务端为准、导出/上传/超时/错误提示统一、ui.ts 的 rpx 改 px。
- **批次 5（状态机与幂等）**：作业截止、抽奖开奖、擂台判定、驳回重提、重复缴纳累加与流水。

## 5. 需要产品决策的点

1. 「在编人数」口径：`left`/`staff`/`system` 是否计入任何分母（当前三种口径并存）。
2. 大额报销是否需要普通学员参与投票？若要，需要同时改权限矩阵、前端入口与阈值口径；若不要，删掉 step3 全员可见与相关注释。
3. 「中队内任一区队干部可平行管理」是否包含**写**（改删他区队内容）？当前只读做了作用域、写没做——需要明确边界。
4. 心理申请可见范围是否收窄为「心理副区/辅导员/超管」。
5. `id=34 殷政` role=8 且 member_type='student' 是刻意兼任还是导入残留；生产库冗余索引 `idx_test` 是否清理。
6. `UPLOAD_AUTH_MODE` 切 strict 的时机（旧 APK 覆盖率）。

## 6. 补齐：请假 / 内容 / 前端页面三个切片的结论（2026-09-11 补录）

### 请假（P0/P1）
- **[P0] 时段校验可绕过**：固定类型只比 HH:MM:SS（不比日期方向），自由类型（中队会/全休/其他）零校验，且无时长上限 → 反向区间、跨年超长、过去时间都能入库；审批通过后 present = member_count - on_leave 被系统性拉低（`services/leaveService.js:22-31`）。
- 审批无状态机/幂等/status 校验（`leaveService.js:87-99`、`models/Leave.js:107-114`，且该路由是唯一没有 validateBody 的写路由）：可重复审批、可审批已销假记录、可写任意 status。
- **干部可自审**（`leaveService.js:89-95`）：role 1-7 未做回避校验。
- 「未销假」恒为 0：autoCancelExpired 与 not_returned_count 谓词逐字相同（`models/Leave.js:126-131` vs `CompanyController.js:73-75`），且把 cancelled_time 写成执行时刻，学员会看到自己没销假的记录显示「已销假」。
- 干部审批页「管理员销假」必然 403（按钮走仅限本人的 `/leave/cancel/:id`）。
- 撤销待审批只写 is_cancelled 不改 status → 幽灵待审批，且列表与两处计数口径不一致。
- 超管控制台请假聚合缺 member_type='student'（`adminConsole.js:52-54,172-173`）。
- todayLocal() 若带 getTimezoneOffset() 在宿主机为东八区时是空操作（本轮已改为无条件 UTC+8）。

### 内容模块（P0/P1）
- **[P0] 积分先写库后鉴权**（`PointsController.js:44-56`）：403 也照样落库、可无限刷；线上已有脏数据（points id 132/133/134 = -100/+1000/-10000，用户 25 合计 -9050）。
- **[P0] 通知完成名单跨区队**（`models/Notice.js:144-146`）：任一区队长拿到全中队 205 人姓名+学号。
- **[P0] 匿名内容经审计日志去匿名化**（本轮已修）。
- 作业截止后仍可提交且覆盖已批改分数；抽奖可重复/对已关闭活动开奖；擂台无唯一键、judge 不校验状态。
- 三个角标计数无区队过滤（`Notice.js:68-77,126-132`、`Homework.js:100-111`）→ 红点数恒大于列表。
- canAccessClassRecord（中队平行**查看**语义）被当**写**权限用：任一区队干部可改删他区队通知/公告/作业/相册/积分。
- 心理申请可见范围过宽（HANDLE_PSYCHOLOGICAL 默认角色 1-9）。

### 前端页面（P0/P1，本轮已修 4 项）
- **[P0] /admin/panel 白屏**：前端权限镜像缺 5 个键 → hasAnyRole(undefined) 抛 TypeError（首次登录/换设备必现）。已补键 + 兜底返回 false。
- **[P0] DDL 与模型漂移**：announcements.is_pinned、challenge_applications.proof_urls/notes 无迁移 → 新环境公告发布/擂台申请必 500；线上「首页置顶」开关永远无效。**待修**。
- 请求层错误提示死代码（已注册 setRequestToastHandler(showToast)）。
- 超管导出按钮必然 401（已改带鉴权 blob 下载）。
- 助手流式路径丢附件（已改为显式传快照）。
- 权限显隐与后端矩阵脱钩：fee/index.vue:13 硬编码 role===2||8、公告/投票/抽奖/擂台用 isAdmin 代替具体权限。**待修**。
- 相册：非管理员上传后无审核入口、详情 includePending 只给管理员 → 照片永久不可见。**待修**。
- 作业：前端无发布/批改入口，submissions 拉取后从不渲染。**待修**。
- 成员详情积分字符串拼接（15+5=155）；积分管理改学号后仍提交给旧用户；成员管理页无分页只显示前 50。
- 「移出统计」实际会清空职务与备注且恢复不还原（文案未说明）。

## 7. 本轮已修复清单（已上线）

1. 用户资料接口越权 → 账号接管链（实测复现后修复）。
2. 助手写操作可绕过二次确认（模块工具）+ MCP 改为两阶段确认。
3. 审计日志泄露匿名内容（敏感模块不落 body + detail 仅超管）。
4. 中队概览「今天」用 UTC（改为无条件 UTC+8）。
5. 前端请求层错误提示死代码 + 401 权限快照残留。
6. 超管后台导出按钮 401 + 前端权限镜像补齐 5 键防白屏。
7. 助手流式丢附件、isWriteCall 重复定义。

## 8. 批次修复进展（2026-09-11 第二轮，已上线）

### 批次 1 班费（已修）
- **作用域**：缴纳/免缴/截止、审批/驳回/投票、待审批列表、公示 全部补区队校验（跨区队 403，列表按区队过滤）。
- **金额**：缴纳金额以服务端「人均应缴」为准（不再接受 0.01/负数），只允许未截止批次，已缴不再静默覆盖（返回明确原因）；免缴不再计入已缴人数与已收金额。
- **门槛**：大额报销投票门槛改为「本区队在编学员数 × 2/3」（原全校人数导致 >500 元报销永久卡死）。
- **事务**：建单+审批链同一事务；审批节点流转、投票、驳回均事务化；驳回会终止后续节点（不再有幽灵待办）。
- **语义**：审批/驳回失败返回 success:false（409），不再误报「已审批通过」。
- **口径**：`total_expected/total_count` 改为本区队在编学员数；`Fee.getBalance` 全部 Number()（修掉字符串拼接）；公示只汇总本区队并纳入收缴收入。
- **入口**：前端改用 `hasPermission('COLLECT_FEE'/'APPROVE_FEE_USE')`；缴纳金额只读；列表显示「已缴/免缴」。

### 批次 3 请假与内容（已修）
- **请假时段**：改真实时间校验（开始<结束、结束>现在、≤30 天、固定时段需同一天）——反向区间/跨年超长/过去时间全部拒绝（已实测 400 文案清晰）。
- **审批状态机**：只有「待审批且未撤销」可被处理（幂等）；审批结果只能是 1/2；**不能审批自己的请假**；跨区队 403。
- **撤回语义**：撤销待审批改为独立状态（status=3），不再产生 status=0 且 is_cancelled=1 的幽灵待审批。
- **销假**：本区队干部可代学员销假；`autoCancelExpired` 已移除（它把「未销假」永久清零并伪造销假时间）。
- **积分**：作用域校验前移到写库之前（实测越权调用 403 且不落库），分值范围校验。
- **通知**：完成名单/未读数/待办数按通知所属区队过滤（原来任一区队干部能拿到全中队姓名学号）。
- **写权限**：7 个控制器的写路径由「平行查看」语义改为「本区队」语义（跨区队改删内容不再可能）。
- **状态机**：作业已批改不可覆盖、抽奖不可重复开奖/对已关闭活动开奖、投票（含多选）先清票再写入。
- **控制台口径**：请假聚合只统计在编学员；成员详情「当前在假」加 start_time<=NOW()、待审批排除已撤销。

### 批次 2 隐私（已修）
- `/uploads` 切 strict（支持 Authorization 头与 ?token= 两种方式，私有缓存）；日志 redact req.url（令牌不再进日志）；`find-by-student` 收敛（不再返回手机号/邮箱）。

### 批次 3c 数据与脚本（已修）
- 迁移 017：补唯一键（作业提交/擂台报名/班费批次/中队名）与 6 个热点索引（重复数据先保留最新再建键；财务与主数据类重复只告警）。
- 迁移 018：补齐「模型在用但 DDL 缺失」的列（lotteries.drawn_at、announcements.is_pinned、challenge_applications.proof_urls/notes）。
- `setup-test-db.js` 加库名白名单 + 动态读取 migrations；`restore-db.js` 加白名单、单事务、时区确定性；`seed.js` 清理单事务、失败即中止、清单补到 38 张表。
- 公告 `is_pinned` 现已透传（首页置顶可用）。

### 仍未完成（下一轮）
- 前端缺失入口：心理申请处理页、班费公示发布入口、「我的挑战申请」入口；作业列表页的发布作业入口。
- 投票「全员表决」的产品口径（是否放开 step3 给学员）待确认。
- 历史脏数据：points 132/133/134（-100/+1000/-10000）、fee_collections.collected_amount=3685.80 与 0 条明细的口径差异需人工核对。
